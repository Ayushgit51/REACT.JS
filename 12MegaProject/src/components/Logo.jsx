import React from "react";

function Logo({ width  = '100px' }) {
    return (
        <div style={{ width }}>{/* You can replace this with an actual logo image or SVG */}
            Logo
        </div>
    );
}

export default Logo;